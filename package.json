{
  "name": "volaciampino",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "rm -rf _site && eleventy",
    "deploy": "npm run build && gh-pages -d _site --add --nojekyll"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-img": "^6.0.4"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
